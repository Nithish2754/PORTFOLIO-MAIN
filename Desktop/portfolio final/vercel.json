{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "^/index\\.html$", "dest": "/index.html" },
    { "src": "^/api/(.*)$", "dest": "/api/$1.js" },
    { "src": "^/(.*)", "dest": "/index.html" }
  ]
}
